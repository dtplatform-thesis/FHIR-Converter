{{#if state}}
	{{#if (eq state '010')}}
		"Piemonte"
	{{else if (eq state '020')}}
		"Valle d'Aosta"
	{{else if (eq state '030')}}
		"Lombardia"
	{{else if (eq state '041')}}
		"Provincia Autonoma di Bolzano"
	{{else if (eq state '042')}}
		"Provincia Autonoma di Trento"
	{{else if (eq state '050')}}
		"Veneto"
	{{else if (eq state '060')}}
		"Friuli Venezia Giulia"
	{{else if (eq state '070')}}
		"Liguria"
	{{else if (eq state '080')}}
		"Emilia Romagna"
	{{else if (eq state '090')}}
		"Toscana"
	{{else if (eq state '100')}}
		"Umbria"
	{{else if (eq state '110')}}
		"Marche"
	{{else if (eq state '120')}}
		"Lazio"
	{{else if (eq state '130')}}
		"Abruzzo"
	{{else if (eq state '140')}}
		"Molise"
	{{else if (eq state '150')}}
		"Campania"
	{{else if (eq state '160')}}
		"Puglia"
	{{else if (eq state '170')}}
		"Basilicata"
	{{else if (eq state '180')}}
		"Calabria"
	{{else if (eq state '190')}}
		"Sicilia"
	{{else}}
		"Sardegna"
	{{/if}}
{{/if}}
